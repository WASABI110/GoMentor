# Final gate — B1–B9 verdicts (2026-09-06)

Recorded at the M2 final gate, per the M1 pattern (commit `a1f2e6c`). Verdicts come from the adversarial read-only verification pass (`gomentor-verify`) over the final tree, cross-checked against the trellis-check pass that preceded it, plus real-engine evidence produced the same day (`research/benchmark-eigen.md`).

State at record time: all gates green on the final tree — lint, typecheck (5 projects), 1321 unit/integration tests, 36 e2e tests (including the packaged launch), 95/95 mutation coverage, format, i18n, licenses, trellis immutability.

| ID | Verdict | Evidence |
|---|---|---|
| B1 | **PASS (win, local)** / **CI-deferred (linux, darwin)** | Windows verified twice against fresh `dist/win-unpacked` packaging: the packaged `GoMentor.exe` reaches `ready` against the real bundled engine (no override) and lands a real ≥450-visit readout in ~4s (`test/e2e/packaged-launch.spec.ts`; the ≥450 is the real engine's NN-batch overshoot of the 500 cap, measured 501 — the fake echoes the cap exactly). Packaged layout asserted on disk: `resources/katago/win32-x64/katago.exe` + VC++ DLLs, weights = the pinned b6c96 only. Linux and macOS halves are wired into CI (the packaged-launch step runs under `xvfb-run` on ubuntu and natively on windows after `pnpm package`; on darwin the same spec asserts `unavailable` by construction and that a record still opens). Not yet observed green — that is the next push's CI run. |
| B2 | PASS, one recorded gap | sha256 verified independently by the verifier (recomputed, matches `scripts/katago-checksums.json`); reuse path verifies size+hash, mismatches are rejected loudly, resume is exercised against a real HTTP server (`scripts/test/fetch-engine.test.ts`). Recorded gap: **`engine:linux-x64` has no recorded sha256** — no Linux machine has fetched, and CI structurally cannot commit the observation. The Linux engine is size-pinned only until someone records the hash. |
| B3 | **PASS** | Deterministic half: `analysis.spec.ts` (cursor-following, candidates pixel-asserted at exact intersections, ownership toggle) + `sweep.spec.ts` (progressive fill, click-to-seek, branch re-sweep). Real half: measured, not estimated — b6c96 on the reference machine: cold start ~0.5s, complete 500-visit focus read ~3.4s (148 visits/s), partials from ~0.1s; independently reproduced by the verifier (531ms / 3.6s / 139–148 v/s). The pre-agreed b10→b6 contingency fired on the measurement (b10: 8.1s per read — outside the live envelope). |
| B4 | PASS | Wrong-length ownership rejected at three layers: protocol unit (81 vs 80 → typed `ENGINE_QUERY_FAILED`), session unit (rejects, session survives), e2e (readout stays empty while neighbouring steps repopulate). Renderer maps row-major with a size² guard that returns, never shifts. Note: length tests use 9×9=81; the check is size-generic. |
| B5 | PASS | Real spawned child `--crash-after`: transitions `starting→ready→starting→ready`, focus re-issued for the same cursor, sweep resumes from the ledger with each move settled **exactly once**; repeated crashes exhaust to `failed(ENGINE_CRASHED)` with real 1s/2s backoff; shutdown cancels a pending respawn (no orphan). |
| B6 | PASS | `--hang-on` trips the watchdog: terminate-all → kill → restart → analysis resumes, sweep not re-queried. Probe-deadline variant uses real timers; `--garbage-on` proves a bad line is neither hang nor crash. |
| B7 | PASS | Bidirectional A9 meta-test over all 4 new channels + 2 events with ≥2 invalid cases each; non-vacuity proven by injecting an uncovered channel into a scratch copy and requiring the meta-assertion to fail. Mutation coverage re-run at the gate: 95/95 (includes M38/M93 for the v1.18 thread split added by the real-engine gate, M94/M95 for the backoff breaker its header claims). |
| B8 | PASS, note | Dev missing-binary degrades to `unavailable` with a fetch hint (e2e); packaged missing → `failed(ENGINE_BINARY_MISSING)` (integration); macOS unsupported → `unavailable` by construction (unit + the darwin packaged gate). Note: the `failed`-state-still-usable half is integration-only, not e2e — the PRD's verification column says e2e; recorded as a known shortcut, not a defect. |
| B9 | **PASS** (as of this record) | The verifier's adversarial pass initially failed B9: no recorded verdicts existed. This file is that record; `implement.md`'s stage checkboxes are ticked with a deviations addendum; the journal carries the session entry. |

## Non-blocking findings carried forward

1. `engine:linux-x64` sha256 unrecorded (see B2).
2. The darwin packaged-launch assertion has never executed (no darwin runner run since it was written) — first validation is the next CI push.
3. `perspective.ts`'s end-to-end sign check against a live White-to-move readout remains verified-by-construction (re-derived from vendored `searchresults.cpp` at this gate); no captured transcript exists, and none is fabricated.
4. Spec-local coupling: any e2e spec that opens a record without an engine override spawns the real engine on machines with fetched resources. `board-render.spec` now suppresses it via a nonexistent `GOMENTOR_KATAGO_BINARY`; other specs open games only through the fake-override or no-game paths.
