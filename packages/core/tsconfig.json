{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // See packages/shared/tsconfig.json — same reasoning.
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
